# v1.0.0 — Short changelog

- Final release v1.0.0 (schema freeze, Windows clean-install verified, all tests green).
- New CLI polish: `health`, `today`, `node`, and unified `report` family; stdlib-pure rendering via src/skilltrace/render.py; aliases `st`/`submit`/`close`.
- Safety rules enforced: no AI-only pass/master/delete; asserted progress immutable.
- Docs added: INSTALL, RUNBOOK, USER_GUIDE, SAFETY_BOUNDARIES, SCHEMA_REFERENCE, CHANGELOG.
- Release assets attached: release manifest, test results, full release artifact zip.

Full release notes: docs/RELEASE_NOTES.md
