# Schemas

This release candidate includes the schema layer as a project contract.

The design layers define Pydantic models for graph, evidence, execution, interface, policy, and release metadata. The bundled compiler uses lightweight runtime validators so the package can run locally with only PyYAML installed.
