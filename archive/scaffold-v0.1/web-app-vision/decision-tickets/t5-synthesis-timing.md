# Decision Ticket T5: Synthesis Document Timing

**Title:** Decide when to generate the synthesis research document

**Description:**
The synthesis research document (docs/reference-research-document.md) combines the original 5-Pillar framework vision with the current skilltrace build, analyzes the interface gap, and defines the integration path. Two timing options:

- **A) Generate now** — produce the document immediately to guide the interface design decisions (T1, T2, T3, T4). This uses the document as a planning artifact before committing to interface direction.

- **B) Generate after interface decision** — wait until T1 (interface type) is resolved, then produce the synthesis document tailored to the chosen interface. Avoids producing work that may need re‑filtering.

**User preference stated:** "Generate now to be used to guide the interface design."

**Decision Needed:** Confirm timing. The user has indicated "Generate now to be used to guide the interface design," so Option A is the default. But formally record the decision.

**Dependencies:**
- T1‑T4 (the synthesis document references all decision tickets; producing it early ensures they're aligned)
- The synthesis document itself (docs/reference-research-document.md — already in planning phase)

**Labels:** decision, timing, priority-medium