"""SkillTrace — a local-first, CLI-first, single-learner learning engine.

This package is the installable `skilltrace` CLI introduced at v0.3 (roadmap
decision 13), replacing the throwaway `compiler/` scaffold retired in v0.4.
"""

__version__ = "0.3.0rc1"
